#!/usr/bin/python3
def element_at(my_list, idx):
    if idx >= 0 and idx < len(my_len):
        return(my_list[idx])
    else:
        None
    #return (my_list[idx] if 0 <= idx < len(my_len) else None)
