#!/usr/bin/python3
""""
contains Is_kind function
"""


def is_kind_of_class(obj, a_class):
    """returns true if the objects is an instance of, otherwise false"""
    return (isinstance(obj, a_class))
